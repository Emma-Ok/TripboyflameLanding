<section class="relative pt-6 pb-0">
  <!-- Botón para abrir el modal -->
  <button
    class="w-full px-6 py-2 border-gray-500 bg-black text-primary font-bold hover:bg-orange-200 transition-all duration-300 transform hover:scale-100"
    id="openModalButton"
  >
    Más sobre mí.
  </button>

  <!-- Modal -->
  <div
    id="modal"
    class="fixed inset-0 bg-neutral-900 bg-opacity-95 flex justify-center items-center z-50 opacity-0 pointer-events-none transition-opacity duration-300"
    aria-hidden="true"
    inert
    role="dialog"
  >
    <div
      id="modalContent"
      class="bg-tripbackground bg-cover bg-center
         w-11/12 sm:w-3/4 md:w-5/12 lg:w-2/5 xl:w-1/3 2xl:w-1/4 max-w-3xl
             overflow-y-auto p-10 rounded-lg shadow-lg transform transition-all duration-300 scale-95 opacity-0"
    >
      <div class="absolute inset-0 bg-ij-black opacity-65 rounded-lg"></div>
      <div class="relative z-10">
        <header>
          <h1 class="text-2xl font-sans font-bold text-primary">Sobre mí</h1>
          <p class="text-gray-400 font-sans font-bold py-1">
            Un saludo, soy Luy, director y productor audiovisual nacido en Medellín, con +4 años de experiencia.
          </p>
        </header>
        <div class="grid gap-4 py-2">
          <p class="text-white font-sans font-bold">
            Me especializo en crear contenido de video y audio de alta calidad para diversas plataformas. Mis habilidades incluyen fotografía, dirección de videos, montaje y edición para la industria musical TRAP.
          </p>
          <p class="text-white font-sans font-bold">
            Con un ojo agudo para los detalles y una pasión por contar historias, me esfuerzo por ofrecer resultados excepcionales que cautiven a la audiencia.
          </p>
        </div>
        <!-- Botón de cerrar -->
        <button
          class="px-3 py-2 font-sans bg-primary text-white rounded-lg hover:bg-red-300 transition-colors"
          id="closeModalButton"
        >
          Cerrar
        </button>
      </div>
    </div>
  </div>

  <!-- Lógica del modal -->
  <script>
    const openModalButton = document.getElementById("openModalButton");
    const closeModalButton = document.getElementById("closeModalButton");
    const modal = document.getElementById("modal");
    const modalContent = document.getElementById("modalContent");
    const body = document.body;

    if (openModalButton && closeModalButton && modal && modalContent) {
      openModalButton.addEventListener("click", () => {
        // Abre el modal: quitar clases que ocultan y agregar las que muestran
        modal.classList.remove("pointer-events-none", "opacity-0", "opacity-100");
        modal.classList.add("opacity-100");
        modalContent.classList.remove("scale-95", "opacity-0");
        modalContent.classList.add("scale-100", "opacity-100");
        modal.removeAttribute("inert");
        modal.removeAttribute("aria-hidden");
        // Bloquear el scroll
        body.classList.add("overflow-hidden");
      });

      closeModalButton.addEventListener("click", () => {
        // Inicia la animación de cierre en el contenido
        modalContent.classList.replace("scale-100", "scale-95");
        modalContent.classList.replace("opacity-100", "opacity-0");
        // Después de la duración de la transición, remueve la clase que lo muestra en el contenedor
        setTimeout(() => {
          modal.classList.remove("opacity-100");
          modal.classList.add("opacity-0", "pointer-events-none");
          modal.setAttribute("inert", "");
          modal.setAttribute("aria-hidden", "true");
          // Desbloquear el scroll
          body.classList.remove("overflow-hidden");
        }, 300);
      });
    }
  </script>
</section>
