<header
  id="header"
  class="w-full h-20 flex justify-center items-center sticky top-0  bg-black z-50 backdrop-blur-xl shadow-md transition-all duration-300"
>

    <img    
      src="/Tripboyflametest.webp"
      alt="logotripboyflame"
      class="h-full w-auto object-contain transition-transform duration-300 "
    />

  <!-- <div
    class="text-primary font-semibold text-center flex-grow text-sm sm:text-lg md:text-xl"
  >
    TRIP BOY FLAME
  </div> -->
</header>

<script>
  const header = document.getElementById("header");
  if (header) {
    const headerImg = header.querySelector("img");
    window.addEventListener("scroll", () => {
      if (headerImg) {
        if (window.scrollY > 20) {
          headerImg.classList.add("scale-110");
        } else {
          headerImg.classList.remove("scale-110");
        }
      }
    });
  }
</script><script>
  const header = document.getElementById("header");
  if (header) {
    const headerImg = header.querySelector("img");
    window.addEventListener("scroll", () => {
      if (headerImg) {
        if (window.scrollY > 20) {
          headerImg.classList.add("scale-110");
        } else {
          headerImg.classList.remove("scale-110");
        }
      }
    });
  }
</script>